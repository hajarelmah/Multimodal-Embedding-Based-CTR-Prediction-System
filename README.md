# Deep-Multimodal-CTR-Prediction-project-
Multimodal CTR prediction pipeline combining text and image item representations. Item embeddings are generated using Sentence-BERT and CLIP, fused and compressed with PCA, then integrated into a deep learning CTR model with attention over user histories. Achieves strong performance with ~0.925 validation AUC.
